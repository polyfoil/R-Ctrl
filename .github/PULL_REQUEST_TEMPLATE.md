## Summary

<!-- Why this change exists. -->

## Test plan

- [ ] `python -m pytest` passes
- [ ] `python -m ruff check .` and `python -m mypy` pass
- [ ] Widget still launches via `python -m rctrl.launch` (or `scripts\Widget.bat`)
